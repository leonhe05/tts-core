# DataSource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/your_database_name?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
spring.datasource.username=your_username
spring.datasource.password=your_password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis Plus Configuration
# 配置 Mapper XML 文件的位置 (如果使用 XML)
# mybatis-plus.mapper-locations=classpath:/mapper/**/*.xml
# 配置实体类所在的包 (Update to domain model package)
mybatis-plus.type-aliases-package=com.leon.domain.apikey.model
# 开启驼峰命名转换 Table_Name -> TableName
mybatis-plus.configuration.map-underscore-to-camel-case=true
# 打印 SQL 日志 (开发环境建议开启)
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl 